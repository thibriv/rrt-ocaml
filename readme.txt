To compile the project, use the command 'make'.
It will create a HTML documentation in the directory 'doc' and you can open 'index.html' to open it.
It will also create the executable 'prog' that you can launch with the command './prog'.